# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# Environment / secrets
.env
*.local

# Competition data (large, provided by DACON — not committed)
data/
submission/data
*.zip

# Cached eval logits/preds (regenerable)
analysis/cache/

# Claude Code local state
.claude/

# Model weights / training artifacts (large binaries)
*.pt
*.pth
*.ckpt
*.bin
*.safetensors
*.pkl
checkpoints/
logs/
wandb/

*.npz
*.npy

# Research notes & papers (not part of the runnable project)
docs/
papers/

# Local command cheat-sheets (env-specific, not committed)
commands
commands_moana

# rsync exclude list (local tooling)
.rsyncignore

# DACON-provided baseline (reference only, not our code)
baseline/

# Local inference outputs
output/
outputs/

# Editor / OS
.vscode/
.idea/
.DS_Store

presentation/notion*/

# submission build dirs (contain >100MB model weights)
build_tta/
# stale sync backup of .git (kyusang_main salvaged to archive/kyusang_main)
.git.synced-backup/
